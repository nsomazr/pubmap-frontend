# Hostinger / split-domain (default): API on api.gre.nileagi.com — requires CORS on backend.
VITE_API_URL=https://api.gre.nileagi.com/api
VITE_APP_URL=https://gre.nileagi.com

# Nginx same-origin proxy only (see deploy/nginx-frontend.example.conf location /api/):
# VITE_API_URL=/api
