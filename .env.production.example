# Hostinger / split-domain (default): API on api.globalresearchexchange.com — requires CORS on backend.
VITE_API_URL=https://api.globalresearchexchange.com/api
VITE_APP_URL=https://globalresearchexchange.com

# Nginx same-origin proxy only (see deploy/nginx-frontend.example.conf location /api/):
# VITE_API_URL=/api
